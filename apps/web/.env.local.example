# Leave unset for local dev — the app auto-detects the API host from
# whatever hostname served the page (localhost, or a LAN IP for phone
# testing). Set this explicitly only when the API lives on a different
# domain than the frontend (e.g. a real deployment).
# NEXT_PUBLIC_API_URL=http://localhost:8000
